//! Read-side of the services layer.

/// Namespace for every read operation exposed to the handlers.
pub struct Query;

impl Query {
    // Add read operations here, e.g.:
    // pub async fn find_subnet_by_id(db: &DbConn, id: i32) -> Result<Option<subnet::Model>, DbErr>
}
