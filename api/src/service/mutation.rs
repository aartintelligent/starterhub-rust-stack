//! Write-side of the services layer.

/// Namespace for every write operation exposed to the handlers.
pub struct Mutation;

impl Mutation {
    // Add write operations here, e.g.:
    // pub async fn create_subnet(db: &DbConn, form_data: subnet::Model) -> Result<subnet::ActiveModel, DbErr>
}
